{
  "name": "timetable-system",
  "version": "0.1.0",
  "private": true,
  "description": "AP课程排课系统",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "pnpm@9.0.0",
  "dependencies": {
    "@ortools-node/cp-sat": "9.15.0-node.0-rc.0",
    "logic-solver": "^2.0.1",
    "xlsx": "^0.18.5"
  }
}
