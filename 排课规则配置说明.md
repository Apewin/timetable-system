# 排课规则配置说明

## 基本原则

系统把条件分成两类：

- **内核硬约束**：不会在界面中关闭。包括教师、学生、教室同一时段不重叠；课时数完整；教室容量和类型匹配；每位学生的已选课程恰好进入一个平行 section；以及已经锁定的课时不被移动。
- **学校规则**：在“约束管理”中以硬规则或软规则配置。硬规则必须满足；软规则用于在可行方案之间评分和选择，不会导致本来可排的课表失败。

这样可以在临时调整校历、教师或课程时改规则，而不需要改求解算法。

## 可配置规则

| 类型 | 适用情形 | 关键参数 |
| --- | --- | --- |
| `fixed_slots` | 固定班会、社团、值日等 | `slots`，`mode: contains/exact` |
| `forbid_slots` | 某教师、教室或课程禁排 | `slots` |
| `preferred_slots` | 上午优先、走班时段优先 | `slots`，建议设为软规则 |
| `max_occurrences_per_day` | 每天同一门课至多一节 | `max` |
| `max_consecutive_lessons` | 限制同一对象连续课时 | `max` |
| `max_consecutive_days_in_period` | 教师不要连续多天第一节 | `period`，`max` |
| `priority` | 实验教师、跨年级教师优先进入求解 | `rank`（越小越优先） |

每条规则可以按 `teacher`、`course`、`class`、`section`、`student`、`room` 或 `global` 作用。目标 ID 留空表示该范围内全部对象；高级筛选支持 JSON，例如：

```json
{"grades":[11,12]}
```

当前数据已经配置了：低周课时课程每天至多一次（硬）；班会、社团和值日固定（硬）；实验教师优先（软）；同时教高二和高三 AP/选修的教师优先（软）；教师连续多天第一节的限制（软）。

## 关于“连续不超过三节”

不能把它直接设为“每个学生全天所有课都不能连续超过三节”的硬规则：当前学生每周 50 节、每天 10 节，若没有空课，该定义在数学上不可能满足。

教务需要先明确它的对象，例如：

- 仅限制同一门课程；
- 仅限制教师；
- 仅限制 AP/选修课程；
- 只把上午、下午分别视为一个连续区段；
- 或允许它作为软规则。

确认对象后，可用 `max_consecutive_lessons` 直接配置，无需改算法。

## 人工调整后的行为

- 修改 AP/选修 section 的教师，会记录为持久的 section 覆盖。
- 学生在同课程平行 section 间转班，会记录为该学生的固定分班意图。
- 如果当前课表能直接容纳调整，系统立即保存；若时段冲突，网页会自动以该调整为前提重新求解全校课表。
- 只有新的完整课表通过独立硬约束校验后，调整才会写入数据。因此下一次重排不会丢失人工指定的教师或转班结果。
