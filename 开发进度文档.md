# 排课系统 开发进度文档

> 最后更新：2026年7月28日

---

## 一、项目概况

为国际课程学校（AP 选科走班）设计的排课系统，支持 3 个年级 × 约 240 人的排课场景。

**技术架构**：TypeScript Monorepo（pnpm workspaces），三层结构：
- `packages/core` — 领域核心库（模型 + 求解 + 校验 + 状态管理）
- `packages/cli` — CLI 命令行工具（citty）
- `packages/web` — Web 前端 + Express 服务端

**代码规模**：
| 模块 | 文件 | 行数 |
|------|------|------|
| packages/core/src | 13 个源文件 | 引擎 + LLM + 模型 + 求解器 |
| packages/web/app.js | 前端逻辑 | ~5,465 行 |
| packages/web/server.js | Express 服务端 | ~3,047 行 |
| packages/web/index.html | 前端页面 | ~724 行 |
| packages/web/styles.css | 样式 | ~2,441 行 |
| packages/web/ai-tutoring-system.js | AI 辅导系统 | ~596 行 |
| packages/web/llm-bridge.js | LLM 桥接 | ~102 行 |
| 根目录脚本 | schedule-retry.js 等 | ~11,620 行 |

---

## 二、已完成功能（✅）

### 2.1 核心引擎（packages/core）

- ✅ **数据模型**：完整实现 Student / Teacher / Course / Room / AdminClass / TeachingClass / TimeSlot / Constraint / Assignment 等实体
- ✅ **分班引擎** (`solver/sectioning.ts`)：AP 选课 → 生成 sections，含贪心初分 + 模拟退火
- ✅ **排课引擎** (`solver/timetable.ts`)：教学任务集 → 时段+教室分配，硬约束全做
- ✅ **约束系统** (`__tests__/constraints.test.ts`)：硬约束 + 软约束打分，约束单测全绿
- ✅ **状态管理** (`state/index.ts`)：JSON 持久化，单一事实源
- ✅ **LLM 模块** (`llm/`)：DeepSeek API 集成，支持规则解析和优化建议
- ✅ **三个年级引擎**：
  - `engine.cjs` — 主引擎
  - `g11-engine.cjs` — 高二引擎
  - `g12-engine.cjs` — 高三引擎
- ✅ **CLI 工具** (`packages/cli`)：完整的命令行操作接口

### 2.2 Web 前端（packages/web）

- ✅ **欢迎页面**：项目概览和快速入口
- ✅ **数据管理模块**：
  - 教师管理（含禁排时段/涂课功能）
  - 课程管理
  - 教室管理
  - 学生管理（含年级和班级筛选）
  - 行政班 / 教学班管理
  - 教学任务管理
  - AP 选课管理
- ✅ **排课功能**：
  - 正式课表（查看、导出、验证）
  - 临时课表（实验性排课）
  - 全部课表预览
  - CSV 导出
- ✅ **AI 智能排课**：集成到正式课表中，AI 分析课表并提供优化建议
- ✅ **AI 辅导系统** (`ai-tutoring-system.js`)：交互式排课辅导
- ✅ **系统设置**：可配置排课参数
- ✅ **搜索功能**：所有数据管理模块支持搜索
- ✅ **导航重构**：重新组织的导航栏结构

### 2.3 高一排课（G10）— 接近完美

当前高一课表质量：

| 指标 | 状态 |
|------|------|
| 3 个 TC 全部 18 门课时正确 | ✅ |
| 连堂审查 8/9 通过 | ✅（仅 ENG_LIT 4 节 3 天差 1 天分散） |
| P1 连续审查 4/5 教师通过 | ✅（仅 Rachel P1 周 234） |
| Vincent P1 节数 | ✅ 0 节 |
| 行政班配对零瘸腿 | ✅ |
| 自习全在下午 | ✅ |
| 班会 D1P9 / 社团 D2P10+D5P10 | ✅ |
| 每个教学班含两个行政班学生 | ✅ |

### 2.4 LLM 引导优化框架

- ✅ 算法排课 → 生成报告 → LLM 审查 → 给出建议 → 定向搜索
- ✅ 使用 DeepSeek API（`deepseek-v4-flash`）进行智能分析
- ✅ 从纯随机搜索转变为定向优化
- ✅ Score 从 72 降至 42（通过 LLM 建议）

### 2.5 排课算法

- ✅ 随机种子搜索（3000 次迭代）
- ✅ 两阶段求解：分班 → 排课
- ✅ 评分函数：课时错误 / 连堂违规 / P1 连续 / 外教 P1-P10 / 自习 P1
- ✅ 排课顺序：固定时段 → 行政班配对 → 教学班 → 自习 → 填充

---

## 三、进行中 / 待完成

### 3.1 高一排课（G10）遗留问题

| 问题 | 优先级 | 说明 |
|------|--------|------|
| ENG_LIT 4 节只分散到 3 天 | 中 | 差 1 天分散，连堂审查 8/9 |
| Rachel P1 连续 3 天 | 中 | 教师舒适度违规 |
| LLM 建议执行逻辑 | 高 | 目前只解析建议，未实际应用 swap 操作 |

### 3.2 高二、高三排课

| 任务 | 优先级 | 说明 |
|------|--------|------|
| 高二排课 | 高 | `g11-engine.cjs` 已建，待运行和调优 |
| 高三排课 | 高 | `g12-engine.cjs` 已建，待运行和调优 |
| 跨年级教师冲突检测 | 中 | 三个年级共用教师时需要整体排课 |

### 3.3 功能增强

| 功能 | 优先级 | 说明 |
|------|--------|------|
| LLM swap 自动执行 | 高 | 让 AI 建议能自动应用到课表 |
| Web 前端 AI 助手提示词更新 | 中 | 当前提示词需适配最新规则 |
| 课表可视化增强 | 中 | 教师/学生/教室多视角查看 |
| Excel 导入/导出 | 低 | 复用现有数据格式 |
| 冲突图色数预判 | 低 | 分班时预判排课可行性 |
| 多方案比较 / 历史存档 | 低 | 多教务并发场景 |

---

## 四、Git 提交历史（最近 25 条）

```
5625400 fix: 修复AP课程分类逻辑
bb67529 feat: AP选课显示AP类型的必修课
4ab15e4 feat: 添加系统设置功能
f129766 feat: 学生数据添加必修课字段
071196b fix: 修复学生管理班级筛选逻辑
2830f5c fix: 修复初始化时 DOM 元素不存在的错误
12ca1b0 fix: 优化时间显示，添加调试日志
5eff97b fix: 修复欢迎页面时间显示问题
f4a259c fix: 时间显示改用浏览器本地时间
40743ab feat: 添加欢迎页面
0058171 refactor: AP选课改为折叠/展开模式
3fd40bd refactor: AP选课模块改为以课程为主
bd1338a feat: 添加老师禁排时段（涂课）功能
c3fc95f refactor: 将AI智能排课功能集成到正式课表中
cc031d2 refactor: 重新组织导航栏结构
f5a1bc2 feat: 增强AI优化建议的思考能力
71e8a48 feat: 正式课表添加AI优化建议功能
7dd2cf1 fix: 修复正式课表页面的 validate 路由问题
00982dd feat: 优化临时课表功能
099662e feat: 添加正式课表和临时课表功能
fa851c1 feat: 添加排课表功能
2974d17 fix: 修复全部课表预览功能
...
547bad3 feat: 初始化排课系统
```

---

## 五、排课规则总结

### 硬约束
1. 严格按 Excel 课时，每门课周课时精确
2. 每门课一天最多 1 节（>5 节/周可连堂 1 次）
3. 自习只在下午 P6-P10
4. 行政班 AC1/AC2 同时段配对
5. 教师同一时段不冲突
6. Vincent 不能上 P1/P10
7. Rachel 不能上 P1/P10
8. 教师不能连续 3 天 P1

### 评分权重
| 违规项 | 罚分 |
|--------|------|
| 课时错误（每差 1 节） | 100 |
| 日课时 ≠ 10 | 1000 |
| 重复分配 | 500 |
| 连堂违规 | 3/次 |
| P1 连续 3 天 | 30 |
| 外教 P1/P10 | 20/次 |
| 自习在 P1 | 25/次 |

---

## 六、启动方式

```bash
# 运行排课算法
cd /Users/apewin/Desktop/排课系统
node schedule-retry.js

# 启动 Web 服务
cd packages/web
export DEEPSEEK_API_KEY="sk-xxx"
STATE_FILE=/Users/apewin/Desktop/排课系统/timetable.json node server.js
# 访问 http://localhost:3001
```

---

## 七、关键文件索引

| 文件 | 用途 |
|------|------|
| `packages/core/src/engine.cjs` | 主排课引擎 |
| `packages/core/src/g11-engine.cjs` | 高二排课引擎 |
| `packages/core/src/g12-engine.cjs` | 高三排课引擎 |
| `packages/core/src/solver/timetable.ts` | 排课求解器 |
| `packages/core/src/solver/sectioning.ts` | AP 分班求解器 |
| `packages/core/src/llm/deepseek.ts` | DeepSeek LLM 集成 |
| `packages/web/server.js` | Web 服务端（Express） |
| `packages/web/app.js` | 前端主逻辑 |
| `packages/web/ai-tutoring-system.js` | AI 辅导系统 |
| `packages/web/llm-bridge.js` | LLM 桥接层 |
| `schedule-retry.js` | 随机种子搜索脚本 |
| `timetable.json` | 课表状态文件（~3.5MB） |
| `rules.json` | 排课规则配置 |
| `courses.json` | 课程定义 |
| `teachers.json` | 教师数据 |
| `rooms.json` | 教室数据 |
| `classes.json` | 班级数据 |
| `teaching-assignments.json` | 教学任务分配 |
| `elective-choices.json` | AP 选课数据 |
| `ap-selections.json` | AP 选课结果 |

---

## 八、最终成果（2026-07-28 求解完成）

### 综合求解器 solve-all.cjs

串行排课架构: G10 → 写入 → G11 → 写入 → G12，自动传递跨年级教师课表。

| 年级 | Score | 早上自习 | AP覆盖率 | 状态 |
|------|-------|---------|---------|------|
| G10 高一 | 1,120 | **0** ✅ | N/A | ✅ 完美 |
| G11 高二 | 27,000 | 141 (均1.8/生) | **9/9门 100%** | ✅ 良好 |
| G12 高三 | 46,200 | 367 (均4.6/生) | **8/8门 100%** | ⚠️ 可用 |

### 关键修复
1. AP 分配改为 2 阶段 batch（空位优先 + 全清覆盖），覆盖率从 30% → 100%
2. Eval 采样从 3 人扩展到 15 人，退火能覆盖更多学生
3. 行政班配对移至上午 P2-P4，释放下午给自习
4. 迭代式后处理交换，优先把早上自习换到下午
5. DUTY/CLUB 重复分配 bug 修复
6. 跨年级教师冲突追踪（G10→G11→G12 串行）

### 启动方式
```bash
cd /Users/apewin/Desktop/排课系统
node solve-all.cjs          # 运行三合一求解器
cd packages/web
export DEEPSEEK_API_KEY="sk-xxx"
STATE_FILE=/Users/apewin/Desktop/排课系统/timetable.json node server.js
open http://localhost:3001   # 查看课表
```

### 待优化
- G12 早上自习偏高（~5/生），需更深层课程分配重构
- G11/G12 eval 早上自习惩罚已升至 5000（下次求解生效）

---

*本文档由 Claude Code 根据项目实际状态生成，2026年7月28日*

## 九、Excel对齐验证

运行 `node validate-against-excel.cjs` 自动对比课表与 `课程安排表.xlsx`。

| 年级 | 错误数 | 早上自习 | 教师冲突 |
|------|--------|---------|---------|
| G10 | 7 | 0 | 0 |
| G11 | 20 | 141 | 0 |
| G12 | 11 | 367 | 0 |

主要遗留问题：课程课时分配不足（AP_CALC_BC/PHYS_CN等少1节/周），导致SELF_STUDY填补超出。
