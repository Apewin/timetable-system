# 排课系统 开发交接说明

> 给 coding agent 的入口文档。先读本文件，再读两份设计文档。

## 文档清单（同目录）
1. `交接说明.md`（本文件）—— 入口、构建顺序、验收标准。
2. `排课系统设计文档.md` —— 做什么/为什么（架构、场景、数据模型、约束目录、技术栈）。
3. `排课系统构建规格.md` —— 怎么写（实体定义、状态 schema、CLI 契约、约束评估表、算法规格）。**实现以本规格为准**，与设计文档冲突时以构建规格为准。

## 一句话目标
为国际课程学校（AP 选科走班，3 年级 × 80 人 = 240 人）做一套排课系统：CLI 是核心操作抓手 + 前端给普通人 + CLI 留 LLM 接入口。先做 Core+CLI 最小闭环。

## 技术栈（已定）
- **TypeScript** monorepo（pnpm workspaces）：`packages/core`（领域库：model+solver+validate+state，纯逻辑无 IO）+ `packages/cli`（Node，citty）+ `packages/web`（后做）。
- 运行：弱 Windows；维护：Mac/Ubuntu。CLI 用 Node 跑或 `bun build` 打单文件。
- 持久化：单文件 `timetable.json`，写时文件锁。

## 必须遵守的硬性设计约束（不要擅自改）
1. **Core 是唯一逻辑源**：CLI/Web/LLM 都调 Core，不各自实现排课逻辑。
2. **以学生为冲突核心**，不是班级。走班冲突 = 同一学生两个任务不能同时段。
3. **两阶段求解**：分班组班引擎 → 排课引擎，解耦，中间可人工调。
4. **排课单位 = 教学任务（带学生名单）**；班级只是"名单生成器"。
5. 走班时段机制：统一走班时段为主 + 保留混排（AP 落走班时段是软目标，混排不违法只扣分）。
6. 教室：行政班/教学班固定教室（pinned）；AP 用学科教室（类型匹配 + 不重叠，排课分配）。
7. CLI 对 LLM 友好：结构化 JSON 输出 + 明确退出码 + 幂等 + `--seed` 可复现，不做交互式菜单。

## 构建顺序（MVP，建议里程碑）
**M0 · 骨架**
- monorepo 三包结构，`core` 导出实体类型 + zod schema；`cli` 能 `tt init` 生成空 `timetable.json`。
- 状态读写集中在 `core/state`（带文件锁）。

**M1 · 数据建模 + 编辑命令**
- 实体：Teacher / Room / Course / Student / AdminClass / TeachingClass / TeachingAssignment / ApSelection / Constraint（字段见构建规格 §1）。
- CLI：`<entity> add/list/edit/rm`，`--json` 输出，退出码。
- `tt validate-input`：引用完整性 / 容量 / 教师超载预检。

**M2 · 必修教学任务生成**
- `tt build-tasks`（或 `solve sections` 触发）：从 `teaching_assignments` 生成必修 `TeachingTask`（room pinned）。

**M3 · 分班引擎**
- 输入选课数据 → AP sections（学生分配 + 教师分配）+ 色数代理 + `overflow_expected`。
- 贪心初分 + 模拟退火，`--seed` 可复现，`--candidates` 多组。
- 写 `ap_sections` + AP `teaching_tasks`。

**M4 · 排课引擎**
- occurrence→slot 变量，room pinned/assign；硬约束 H1–H8 + 软约束 S1/S2/S3/S5（默认权重见规格 §5）。
- 模拟退火，`--timeout`/`--seed`/`--keep`/`--respect-locks`。
- 不可行返回 best-effort + `hard_violations[]`，exit 4。

**M5 · 查看/校验/手动调/导出**
- `tt validate`、`tt show`（按 学生/老师/班级/教室）、`tt lock/unlock/swap`、`tt export`（csv/html/ical，格式自定）。

**M6（后置）· 独立小模块**：早自习/晚自习排老师值班（类似排监考，不进学生排课引擎）。

## 验收标准（MVP 完成 = 以下全过）
1. 能建全实体并 `validate-input` 通过。
2. `tt solve sections` 出 AP sections，色数 ≤ 走班时段数（不溢出）。
3. `tt solve timetable` 输出课表：**所有硬约束 H1–H8 零违反**（exit 0 或 1），软约束有得分。
4. `tt show` 能按学生/老师/班级/教室看课表。
5. `tt lock` 一节课后重排，锁定不被改。
6. 同输入同 `--seed` → 同输出。
7. 约束单测全绿；小规模手算样例对拍通过。

## 不要做（MVP 外，别顺手加）
- Web 前端、Tauri 桌面、MCP server、多 agent 并发锁、Excel 导入、电子班牌/手机端、多方案历史存档、调代课。
- 早自习/晚自习模块（M6 单独做）。
- 后期软约束 S6/S7/S8/S9（avoid_slot/互斥/教师规则扩展/教研禁排）——模型已预留，先不实现评估。

## 约束编号速查（见构建规格 §5）
- 硬：H1 老师不重叠 / H2 学生不重叠 / H3 教室不重叠 / H4 教室容量 / H5 课时排满 / H6 禁排 / H7 教室类型匹配 / H8 教师日上限 / H9 指定时段 require_slot(后期)
- 软(MVP)：S1 优先上午 / S2 连堂 / S3 AP落走班时段 / S5 课表分散均衡

## 开工第一步建议
搭 M0 骨架 → 跑通 `tt init` → 实现 `core` 的实体 schema + state 读写 → M1 编辑命令 → 用 1 个年级的小样例数据自测。先不碰求解器，把"建数据 + 校验 + 状态"这条链跑通，再加分班/排课。

## 联系上下文
- 场景与决策细节见 `排课系统设计文档.md`。
- 任何"怎么表示/怎么算/命令长什么样"的疑问，先查 `排课系统构建规格.md`，已覆盖实体、状态 schema、CLI 契约、约束评估、算法规格、不可行输出、测试策略。
- 仍待录入（运行时数据，不阻塞）：老师/教室数量、AP 门类与课时/容量/可教老师数、走班时段具体槽位、必修课归属明细——这些进 `timetable.json` 即可。
