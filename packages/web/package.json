{
  "name": "@timetable/web",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "server:legacy": "node server.js",
    "start": "concurrently --kill-others-on-fail \"pnpm --dir ../backend start\" \"vite\"",
    "build": "vite build",
    "test": "node --test test/**/*.test.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "@timetable/core": "workspace:*",
    "cors": "^2.8.5",
    "express": "^4.18.0",
    "multer": "^2.2.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
