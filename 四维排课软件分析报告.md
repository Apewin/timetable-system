# 四维排课软件 (Telossoft K12VC) 逆向分析报告

> 分析时间：2026-07-28
> 软件版本：排课系统_专业版 (.NET Framework 4.7.2)

---

## 一、技术栈

| 组件 | 技术 |
|------|------|
| 运行时 | .NET Framework 4.7.2 (Windows) |
| UI框架 | Krypton Toolkit (WinForms) |
| 数学计算 | MathNet.Numerics |
| 图形渲染 | SkiaSharp |
| Excel处理 | Aspose.Cells + NPOI |
| 序列化 | Newtonsoft.Json |
| 汉字拼音 | SGLibrary.Hzpy.exe |
| 二维码 | QRCoder |
| 压缩 | ICSharpCode.SharpZipLib |

## 二、核心架构（三层）

```
Telossoft.K12VC.ClientUI.dll          (5.8MB) — 表示层
Telossoft.K12VC.ClientBizLogic.dll    (619KB) — 业务逻辑层 ⭐核心
Telossoft.K12VC.Model.dll             (384KB) — 数据模型层
Telossoft.K12VC.ScheduleModel.dll     (52KB)  — 排课模型
Telossoft.K12JW.Engine.Model.dll      (20KB)  — 引擎模型
Telossoft.K12JW.Model.dll             (311KB) — JW数据模型
```

## 三、排课算法核心 — 可借鉴设计

### 3.1 约束系统架构

```
IRuleConstrainter (接口)
    └── BaseConstrainter (基类)
            └── RuleConstraint (实现)
                    ├── AbsRule (抽象规则定义)
                    │     ├── AbsRuleCommon
                    │     ├── AbsRuleProvider
                    │     └── AbsRuleValues
                    ├── EntityRuleKind (规则种类枚举)
                    ├── DtRuleCounter (规则违规计数器)
                    ├── DtRuleAtCell (单元格级规则)
                    └── DtLessonAbsRule (课程抽象规则)
```

**关键设计**：
- `ConstraintName` / `ConstraintOrder` — 规则有名称和优先级排序
- `IsCompelRule` — 强制规则 vs 警告规则
- `HasAbsRule` — 规则是否存在于某实体
- `ClearRuleInvalid` — 清除失效规则
- `RebuildRuleConstraints` — 动态重建约束

**可借鉴 →** 我们当前的 rules.json 缺少优先级排序和规则计数器。应加上 `order` 字段和违规追踪。

### 3.2 随机排课算法 (DoRandomSchedule)

```
Random / ExRandom
    └── DoRandomSchedule
            ├── RandomLsnSetResource (随机分配课程资源)
            ├── DtActSwapOperation (交换操作)
            │     ├── DtActSwapAnalysisResult (交换分析)
            │     └── DtActSwapRuleCounter (交换后规则计数)
            └── GetSwapAnalysisResult (获取交换效果评估)
```

**关键设计**：
- 随机生成初始解 → 交换操作 → 分析结果 → 接受/拒绝
- 每次交换后重新计算规则违规数
- 交换分析结果包含：改善的规则数、恶化的规则数、净收益

**可借鉴 →** 我们的 anneal 缺少结构化的交换分析。应添加：
- `SwapAnalysisResult` 记录每次交换的影响
- 多维度评估（不只看总分，看各规则分项）

### 3.3 时段模型 (TimeMatrix)

```
VcTimeRect (时间矩形)
    └── VcTimeMatrixArr<T> (时间矩阵数组)
    └── VcTimeMatrixDict<T> (时间矩阵字典)
            └── cellTime (单元格时间)
            └── GetScheduleCell_Day / GetScheduleCell_Section
```

**关键设计**：
- 时间用矩阵表示：行=天，列=节
- `VcTimeRect` 表示连续的时间块（用于连堂等场景）
- `TimeBind` — 时间绑定（将课程绑定到特定时间区域）
- `GroupOfTimeArea` — 时间区域组（如"上午"、"下午"、"走班时段"）
- `TimeAreaActCount` — 时间区域内活动计数

**可借鉴 →** 我们的时段模型太简单。应引入：
- `TimeRect` 概念用于连堂识别和约束
- `TimeArea` 分组（morning/afternoon/walk_block）
- 矩阵化时间表示

### 3.4 课程安排与锁定

```
ScheduleManagerImpl
    ├── DoRandomSchedule (随机排课)
    ├── ClearAllPreSchedule (清除预排)
    ├── ClearFixedSchedule (清除固定)
    ├── BreakScheduleEditData (断开编辑数据)
    └── CheckScheduleEditData (检查编辑数据)
```

**关键设计**：
- Fixed/Preset 概念：可以锁定部分课表，重新排其余部分
- `ClearFixedSchedule` vs `ClearAllPreSchedule` 两种清除粒度
- 支持增量式排课

**可借鉴 →** 添加 lock/unlock 机制，支持部分重排。

### 3.5 规则违规与警告系统

```
DtFailedAndWarnItem
    ├── IsCompelRule (强制/硬约束)
    ├── IsCapacityWarn (容量警告)
    ├── RuleCounter (违规计数)
    │     ├── RuleCounter_Now (当前计数)
    │     └── RuleCounter_AfterOP (操作后计数)
    └── ListCanFilterWarnConstraints (可过滤的警告约束)
```

**关键设计**：
- 硬约束 vs 软约束（Compel vs Warn）
- 规则计数：Before/After 操作
- 警告可过滤
- 容量类约束单独标记

**可借鉴 →** 我们已有 scoring，但缺少：
- 操作前后的规则对比
- 分类警告系统
- 可过滤的违规列表

### 3.6 教师与课程关联

```
DtTeacher2Mt / DtTeacherLsnCount
    ├── DtTchGradeSubjectRln (教师-年级-学科关联)
    └── EntityTimeRuleCounter (教师时间规则计数)
```

**关键设计**：
- 教师与多条课程关联
- 按年级-学科建立关系
- 教师时间违规单独追踪

---

## 四、可立即实施的改进

### 优先级 P0 — 核心算法增强

1. **约束优先级排序** — rules.json 增加 `order` 字段
2. **规则计数器** — 每次 anneal swap 后报告分项违规变化
3. **交换分析结构** — DtActSwapAnalysisResult 等价实现

### 优先级 P1 — 功能增强

4. **课程锁定** — lock/unlock 特定 assignment
5. **TimeArea 分组** — 正式的"上午/下午/走班时段"概念
6. **固定课表** — 支持 preset + random fill remaining

### 优先级 P2 — 架构改进

7. **矩阵化时间模型** — VcTimeMatrix 风格的双索引访问
8. **AbsRule 抽象规则** — 规则定义与实现分离
9. **增量排课** — 只重排部分课程

---

## 五、与我们的系统对比

| 特性 | 四维排课 | 我们的系统 | 差距 |
|------|---------|-----------|------|
| 约束模型 | 命名+排序+计数 | JSON规则+评分 | 缺排序和计数 |
| 求解策略 | 随机+交换分析 | 随机+退火 | 缺交换分析 |
| 时段模型 | TimeMatrix+TimeRect | 简单DxPy | 缺矩形/区域 |
| 锁定机制 | Fixed/Preset | 无 | 缺少 |
| 规则反馈 | 分项计数+过滤 | 总分 | 缺少 |
| 教师追踪 | 年级-学科关联 | 跨年级追踪 | 基本持平 |

---

## 六、实施计划

基于第一性原理，最关键的改进是：

### 第一步：规则系统升级
- rules.json 增加 `order`、`category`、`isCompel` 字段
- 添加 `RuleCounter` 到每个引擎的 eval 中
- 每次 swap 后报告分项变化

### 第二步：交换分析
- 添加 `SwapAnalysis` 记录到 anneal 中
- swap 前后各规则分项对比
- 定向优化决策

### 第三步：TimeArea 概念
- 定义 morning(1-5) / afternoon(6-10) / walk_block 区域
- 约束引用时间区域而非具体时段

---

*报告由 Claude Code 在对 Telossoft .NET DLL 逆向分析后生成*
