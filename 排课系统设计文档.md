# 排课系统 设计文档 v0.1

> 状态：设计层关键决策已闭环。本文为 Core 开发依据。运行时数据（老师/教室数量、AP 门类等）为录入项，不阻塞设计。

## 1. 目标与定位

为国际课程学校（AP 选科走班）做一套排课系统，减轻教务排课压力与时间成本。

- **核心**：CLI 是操作抓手，普通人用前端 UI 操作，CLI 留 LLM 接入口，未来可接各种 agent。
- **参照**：telossoft 四维排课免费版（其免费版不支持走班/合班/分层/自动排场地，本系统需覆盖这些）。
- **减负落点**：LLM/算法出草稿，教务只做最终确认与微调。

## 2. 场景

- **规模**：3 个年级 × 80 人 = 240 人。三年级若共用老师/教室须当整体排（仍属小规模，启发式秒级）。
- **双轨班级 + AP 选科走班**：
  - 行政班 ×2（每年级，固定学生分组，上部分必修课，有固定教室）。
  - 教学班 ×3（每年级，固定学生分组，上另一部分必修课，有固定教室；组内学生 AP 选课各不同）。
  - AP 选修（动态分班：按选课人数决定每门 AP 开几个 section，人多拆、人少合）。
  - 学生 = 1 行政班 + 1 教学班 + 多个 AP section。
- **冲突以「学生」为核心**，不是班级（走班核心区别）。
- **运行**：性能不强的 Windows；代码维护在 Mac/Ubuntu。

## 3. 系统架构（三层）

`headless core + 多端接入`（同构于 git / kubectl / 四维云后端+客户端）。

```
客户端层：前端 UI（普通人手动编辑） · LLM Agent · 其他 Agents
                │ 调用
接入层：  CLI（薄壳，结构化 JSON + 退出码） · MCP Server（可选，给 Agent 框架即插即用）
                │ 调用
核心层：  Domain Core（系统大脑，排课逻辑只有这一份）
          ├ 状态模型（单一事实源：JSON / SQLite）
          ├ 分班引擎（形成 AP sections → 带名单的教学任务集）
          ├ 排课引擎（教学任务集 → 时段+教室 → 无冲突课表）
          └ 校验 + 评分（硬约束合法性 / 软约束打分）
```

**要点**：核心是 Domain Core 库，CLI/MCP 只是壳；前端/LLM/Agents 都是 Core 的客户端，共享同一状态源。保证"怎么排课"只有一份逻辑，结果永远一致。

## 4. 数据模型

### 4.1 核心实体
- `Student`：id, admin_class_id, teaching_class_id, ap_selections[](选的 AP 课 id 列表)
- `AdminClass`（固定）：id, name, fixed_room_id
- `TeachingClass`（固定学生分组，非按 AP）：id, name, fixed_room_id, student_ids[]
- `ApSection`（动态生成）：id, course_id, teacher_id, room_id(分配), student_ids[], capacity
- `TeachingTask`（排课单位）：id, course_id, teacher_id, student_ids[](名单), weekly_hours, room_policy(pinned固定教室 / assign分配)
- `Course`：id, name, type(必修/AP), required_room_type?, weekly_hours
- `Room`：id, name, type(普通/物理/化学/计算机/…), capacity, owner_class_id?(固定归属)
- `TimeSlot`：id, day(1-5), period(1-10), session(AM/PM), is_walk_block
- `Constraint`：type, scope, params
- `Assignment`：task_id → (timeslot_id, room_id)

### 4.2 关键原则
- **班级不是学生的单一属性**：学生同时属行政班+教学班+多个 AP section。
- **排课单位 = 教学任务（带学生名单）**；行政班/教学班/AP组只是"名单生成器"（录入时帮某门课带出学生名单）。
- **冲突 = 同一学生的两个任务不能落在同时段**。
- AP section 是分班引擎产出，其 student_ids 来自选课数据 + 分班结果。

## 5. 时段模型

- 单周课表（不循环）。
- 一周 5 天 × 每天 10 节（上午 5 + 下午 5）= **50 正课时段/周**。
- 午休默认不可排（如可排另行配置）。
- **走班时段块**：教务配置若干时段标记 `is_walk_block=true`；AP 课优先排走班时段。
- 早自习 / 晚自习：只排老师值班、不排学生课 → **独立子模块**（类似排监考），不进学生排课引擎。

## 6. 教室模型

- **行政班 / 教学班**：各有固定教室，必修课用该固定教室（room pinned，不分配）。
- **AP 选修**：用学科教室，按课程 `required_room_type` 匹配 + 教室不重叠，由排课引擎分配。
- 排课输出含教室分配（任务 → 时段 + 教室）。
- 这是四维免费版不支持的"自动排场地"，本系统要做。

## 7. 两阶段求解

```
选课数据 → [分班组班引擎] → 教学任务集(带名单, 可查看可手动调) → [排课引擎] → 无冲突课表
```

1. **分班组班引擎**：选课数据 → AP sections（每门 AP 几个 section + 学生分配），产出带名单的教学任务集。可单独运行、人工调。
2. **排课引擎**：教学任务集 → 分配时段 + 教室 → 无冲突课表。
- 两阶段解耦：分班可先跑、人看、手动调，满意了再排课。
- 耦合：分班质量用「冲突图色数」预判排课可行性，避免分完班排不出来。

### 7.1 AP 分班「最理想逻辑」
- **硬约束**：每学生每选课 AP 恰分 1 个 section；section 人数 ≤ 教室容量；section 数 ≤ 可教老师数。
- **软目标**：section 人数均衡（方差小）；降冲突压力（最小化冲突图色数）；选课组合相同者尽量同套 section 便于并行。
- **算法（240 人）**：贪心初分 + 模拟退火局部搜索，目标 = 加权和，评估用色数代理不真跑排课。秒级，可出多组候选。

### 7.2 走班时段机制
- 统一走班时段为主：AP 优先排走班时段。
- 保留混排可能：走班时段装不下 / 特殊安排时 AP 可溢出到普通时段，靠学生级冲突检测保证不撞——不违法，只扣分。
- 实现：`is_walk_block` 标记 + "AP 落走班时段"作软目标打分。

## 8. 约束目录（对照四维免费版）

### 硬约束（违反 = 课表非法，MVP 全做）
| 约束 | 说明 | 四维免费版 |
|---|---|---|
| 老师不重叠 | 同老师同时段只一课 | ✅ |
| 学生不重叠 | 同学生同时段只一任务（走班核心） | ✅ |
| 教室不重叠 | 同教室同时段只一课 | ✅ |
| 教室容量 | section 人数 ≤ 容量 | ✅ |
| 课时排满 | 每门周课时排足 | ✅ |
| 禁排（硬） | 某课/师/班禁某时段 | ✅ |
| **学科教室类型匹配** | AP 课须排到匹配类型教室 | ❌ 免费无（本系统做） |

### 软约束（打分优化）
| 约束 | 四维 | 阶段 |
|---|---|---|
| 优先排上午（主课优先上午） | ✅ | MVP |
| 连堂（2–4 节） | ✅ | MVP |
| AP 落走班时段 | — | MVP |
| 教师日上限 | ✅ | MVP |
| 课表分散均衡 | ✅ | MVP |
| 尽量不排 | ✅ | 后期 |
| 多教师互斥 | ✅ | 后期 |
| 教师规则（任意教师附加，含班主任；指定时段=require_slot） | ✅ | 后期 |
| 教研活动禁排 | ✅ | 后期 |
| 主副课交叉 | ❌ | 后期 |

### 独立模块（不在排课引擎内）
- 早自习 / 晚自习 排老师值班（类似排监考）。

## 9. 技术栈

- **TypeScript**（倾向，待最终确认）：Core / CLI / 前端同语言、共享类型、一份代码三处用。
  - 代价：无 OR-Tools 等成熟求解库；但 240 人手写 CSP + 模拟退火够用。
  - CLI：Node（citty/commander）。
  - 前端：Web（Core 跑 Web Worker 不卡 UI）；要桌面再 Tauri 包一层（弱 Windows 排除 Electron）。
- 持久化：先 JSON，并发需求上来再换 SQLite + 事务。
- 多 agent 并发写状态需锁/事务。

## 10. CLI 设计原则（LLM 可操作）

- 语义化子命令 + 显式参数，不做交互式菜单（LLM 没法选菜单）。
- 默认结构化输出（JSON），退出码明确（0=成功，非 0=冲突/错误）。
- 幂等：同样命令结果一致；`solve` 不破坏已 `lock` 的排课。
- 可探查：`status`/`show` 随时读回当前状态，形成"感知→决策→执行"闭环。
- 命令示例：`init` / `add`·`edit`·`rm`·`list`（各实体）/ `solve`(分班+排课，可分阶段) / `show` / `export` / `lock`·`swap`。

## 11. 未定 / 延后项

- 具体运行时数据（老师总数、教室数与类型、AP 门类与课时、各 AP 容量与可教老师数）→ 录入项。
- 必修课归属（哪门必修在行政班 / 教学班）→ 录入时定，模型已支持。
- Excel 导入（是否复用四维教师分工表格式）。
- 课表输出对象（学生/老师/班级/总表）与格式（Excel/PDF/电子班牌/手机）。
- LLM 主要用途（对话式建数据 / 自然语言调课 / 仅求解）。
- 多教务并发 + 多方案/历史存档。

## 12. MVP 范围

- 数据建模 + 编辑命令（建学生/行政班/教学班/选课/课程/教室/约束）。
- 分班组班引擎（AP sections）。
- 排课引擎（时段 + 教室分配，硬约束全做 + 5 个核心软约束）。
- 校验 + 冲突报告 + JSON 输出。
- `show` 可视化课表。
- 独立小模块：早自习/晚自习排老师值班（可后置）。

## 13. 关键设计决策摘要

| 项 | 决策 |
|---|---|
| 架构 | headless core + 多端；Core 库是核心，CLI/MCP 是壳 |
| 数据模型 | 学生 + 教学任务（带名单）为中心；班级 = 名单生成器 |
| 求解 | 两阶段：分班 → 排课；色数代理预判可行性 |
| 时段 | 单周 5×10=50 槽；走班时段块可配置 |
| 教室 | 行政班/教学班固定教室；AP 学科教室类型匹配分配 |
| 走班 | 统一走班时段 + 保留混排（软目标） |
| 规模 | 240 人，启发式秒级，不上 ILP/分布式 |
| 技术栈 | TypeScript（Core/CLI/前端同语言） |
| LLM 抓手 | CLI 结构化 JSON + 退出码，可选 MCP |
