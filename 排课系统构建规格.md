# 排课系统 构建规格 v0.1（Build Spec）

> 与《排课系统设计文档 v0.1》配套：设计文档讲"做什么/为什么"，本文讲"怎么构建到能照着写代码"。本文为准构建依据，冲突时以本文为准。

---

## 1. 实体完整定义

字段标注：`T` 类型，`?` 可选，`[]` 数组，枚举用 `|`。

### 1.1 Teacher（补漏）
```
Teacher {
  id: string
  name: string
  grade?: 1|2|3              // 主要任教年级（跨年级可多填 can_teach 体现）
  can_teach: CourseId[]      // 能教的课程
  available_slots: SlotId[] // 可排时段；默认全部 50 槽；教研/请假从中剔除
  max_per_day: number       // 每天上限（默认 8）
  max_per_week: number      // 每周上限（默认 30）
  homeroom_class_id?: AdminClassId  // 是否班主任
}
```

### 1.2 TeachingAssignment（教师分工——必修课数据入口，补漏）
这是生成必修课 `TeachingTask` 的源头数据，对应四维"教师分工表"。
```
TeachingAssignment {
  id: string
  teacher_id: TeacherId
  course_id: CourseId
  class_id: AdminClassId | TeachingClassId
  class_type: "admin" | "teaching"
  weekly_hours: number      // 每周课时
}
```
**生成规则**：每条 `TeachingAssignment` → 1 个 `TeachingTask`：
- `student_ids` = 该班学生名单
- `room_policy` = `"pinned"`，`room_id` = 该班 `fixed_room_id`
- `weekly_hours` 透传

### 1.3 Course
```
Course {
  id: string
  name: string
  type: "required" | "ap"            // 必修 / AP选修
  required_room_type?: RoomType       // AP课指定学科教室类型；必修通常 null（用固定教室）
  weekly_hours: number               // 默认周课时（可被 TeachingAssignment 覆盖）
  prefer_morning?: boolean           // 主课优先上午（软）
  consecutive?: {min:number, max:number}  // 连堂需求
}
```

### 1.4 Room
```
Room {
  id: string
  name: string
  type: RoomType
  capacity: number
  owner_class_id?: AdminClassId | TeachingClassId  // 固定归属（行政班/教学班的固定教室）
}
RoomType = "general" | "physics" | "chemistry" | "biology" | "computer" | "art" | "music" | string  // 可扩展
```

### 1.5 Student / AdminClass / TeachingClass
```
Student { id, name, grade:1|2|3, admin_class_id, teaching_class_id }
AdminClass    { id, name, grade, fixed_room_id, student_ids[] }
TeachingClass { id, name, grade, fixed_room_id, student_ids[] }   // 固定学生分组，非按AP
```

### 1.6 ApSelection（选课数据——分班引擎输入）
```
ApSelection { student_id: StudentId, course_ids: CourseId[] }   // 该生选的 AP（3门）
```

### 1.7 ApSection（分班引擎产出）
```
ApSection {
  id: string
  course_id: CourseId
  teacher_id: TeacherId
  student_ids: StudentId[]
  room_type: RoomType            // = course.required_room_type
  capacity: number
}
```
**生成规则**：每个 `ApSection` → 1 个 `TeachingTask`（`room_policy="assign"`，`weekly_hours`=course.weekly_hours）。

### 1.8 TeachingTask（排课单位）
```
TeachingTask {
  id: string
  source: "required" | "ap"        // 来自教师分工 / 来自分班
  course_id, teacher_id
  student_ids: StudentId[]
  weekly_hours: number
  room_policy: "pinned" | "assign"
  room_id?: RoomId                 // pinned 时固定；assign 时由排课填
  source_class_id?: AdminClassId|TeachingClassId  // 必修来源班
  source_section_id?: ApSectionId  // AP 来源
}
```

### 1.9 TimeSlot
```
TimeSlot { id: "D{day}P{period}", day:1..5, period:1..10, session:"AM"|"PM", is_walk_block:boolean }
```
`is_walk_block` 由 `state.config.walk_blocks` 派生。

### 1.10 Constraint
```
Constraint {
  id: string
  type: ConstraintType
  scope: "teacher"|"course"|"class"|"student"|"task"|"global"
  target_id?: string
  params: object      // 见约束评估表
  weight?: number     // 软约束权重（默认 1）
  hard: boolean       // true=硬，false=软
}
ConstraintType =
  "forbidden_slot" | "avoid_slot" | "prefer_morning" | "consecutive"
  | "teacher_max_per_day" | "spread_balance" | "teacher_mutual_exclusive"
  | "require_slot" | "research_activity_block"
// 无单独"班主任规则"特例：任意教师（含班主任）的需求都用 scope=teacher 的约束表达——
// 禁排=forbidden_slot、指定时段上课=require_slot、互斥=teacher_mutual_exclusive、上限=teacher_max_per_day。
// 规则可附在任意 scope（teacher/course/class/student/task），不止教师；一个对象可挂多条。
```

### 1.11 Assignment（排课结果）
```
Assignment { task_id: TaskId, slot_id: SlotId, room_id: RoomId }
```
注：`weekly_hours>1` 的任务在求解内部拆成 N 个 occurrence；状态层按 task 存多条 Assignment（每条一个 slot）。

---

## 2. 状态文件 Schema（单一事实源）

文件：`timetable.json`（项目根）。单文件，写时加文件锁。
```json
{
  "version": "0.1",
  "meta": { "school": "", "created_at": "", "updated_at": "" },
  "config": {
    "time_model": { "days": 5, "periods_per_day": 10, "lunch_break_after_period": 5 },
    "walk_blocks": ["D1P6","D1P7","D3P6","D3P7","D5P6","D5P7"]
  },
  "teachers": [],
  "rooms": [],
  "courses": [],
  "students": [],
  "admin_classes": [],
  "teaching_classes": [],
  "teaching_assignments": [],
  "ap_selections": [],
  "constraints": [],
  "ap_sections": null,
  "teaching_tasks": null,
  "assignments": null,
  "locks": []
}
```
- `ap_sections / teaching_tasks / assignments` 在对应阶段运行前为 `null`。
- `locks`: `{ "task_id": "...", "slot_id": "D1P2" }[]`，排课尊重。

---

## 3. 数据流（两阶段输入输出）

```
教师分工(teaching_assignments) ──┐
                                 ├─→ 必修 TeachingTask (room pinned)
选课(ap_selections) ─→ [分班引擎] ─→ AP sections ─→ AP TeachingTask (room assign)
                                 ├─→ 合并为 teaching_tasks（教学任务集）
                                 └─→ [排课引擎] ─→ assignments (task→slot+room)
```
- 分班只产 AP 部分；必修 task 由 `teaching_assignments` 静态生成（`tt build-tasks` 或 `tt solve sections` 触发）。
- 教学任务集 = 必修 tasks + AP tasks，写入 `state.teaching_tasks`。
- 排课读 `teaching_tasks` + `locks` → 写 `assignments`。

---

## 4. CLI 命令契约

### 4.1 通用约定
- 默认人类可读表格输出；`--json` 强制结构化 JSON。
- **JSON 信封**：
  ```json
  { "ok": true|false, "data": {...}|null, "errors": [{"code","msg","refs"}]|null, "warnings": [...]|null }
  ```
- **退出码**：`0` 成功 ｜ `1` 存在冲突/违规（但已出解） ｜ `2` 参数非法 ｜ `3` 实体未找到 ｜ `4` 不可行/无解 ｜ `10` 内部错误。
- 所有 ID 为字符串，时段 ID 形如 `D1P3`。

### 4.2 命令表

| 命令 | 参数 | 输出 data | 退出码要点 |
|---|---|---|---|
| `tt init` | `--name?` | `{project_path}` | 0 |
| `tt status` | — | `{counts, last_stage, hard_violations, soft_score}` | 1 若有违规 |
| `tt <entity> add` | 见下 | 新建实体 | 0/2 |
| `tt <entity> list` | `--grade? --json?` | `[entity]` | 0 |
| `tt <entity> edit` | `--id <字段…>` | 更新后实体 | 0/3 |
| `tt <entity> rm` | `--id` | `{removed}` | 0/3 |
| `tt config set-walk-blocks` | `--slots D1P6,D1P7` | `{walk_blocks}` | 0 |
| `tt solve sections` | `--json? --candidates? --seed?` | `{ap_sections, color_number, walk_slot_count, overflow_expected}` | 0/4 |
| `tt solve timetable` | `--json? --timeout? --seed? --keep? --respect-locks` | `{ok, hard_violations, soft_score, assignments_count}` | 0/1/4 |
| `tt solve` | （两阶段，可带 `--seed`） | 上面两者合并 | 0/1/4 |
| `tt validate` | — | `{legal, hard_violations, soft_score}` | 0/1 |
| `tt validate-input` | — | `{ok, missing_refs[], capacity_issues[], teacher_overload[]}` | 0/2 |
| `tt show` | `--by student\|teacher\|class\|room --id? --format table\|json` | 课表视图 | 0 |
| `tt lock` | `--task <id> --slot D1P2` | `{lock}` | 0 |
| `tt unlock` | `--task <id>` | `{removed}` | 0 |
| `tt swap` | `--task <id> --to D1P2` | 重算后的冲突 | 0/1 |
| `tt export` | `--format csv\|html\|ical --by?` | 文件路径 | 0 |

`<entity>` ∈ teacher / room / course / student / admin-class / teaching-class / teaching-assignment / ap-selection / constraint。

### 4.3 关键 add 参数示例
```
tt teacher add --id T1 --name 张伟 --grade 1 --can-teach MATH,PHYS --max-per-day 6 --unavailable D2P1,D2P2
tt room add --id R1 --name 物理1 --type physics --capacity 30
tt course add --id AP_PHYS --name "AP物理" --type ap --required-room-type physics --weekly-hours 4 --consecutive-min 2 --consecutive-max 2
tt admin-class add --id AC1 --grade 1 --name 高一1班 --fixed-room R9 --students S1,S2,S3
tt teaching-assignment add --id TA1 --teacher T1 --course MATH --class AC1 --class-type admin --hours 4
tt ap-selection add --student S1 --courses AP_PHYS,AP_ECON,AP_CS
tt constraint add --type forbidden_slot --scope teacher --target T1 --params '{"slots":["D5P1","D5P2"]}' --hard
```

---

## 5. 约束评估表

| ID | 名称 | 硬/软 | scope | params | 评估（违反/罚） | MVP |
|---|---|---|---|---|---|---|
| H1 | 老师不重叠 | 硬 | task | — | 两 task 同 teacher 且有共同 slot → violation | ✅ |
| H2 | 学生不重叠 | 硬 | task | — | 两 task 共享 student 且同 slot → violation（走班核心） | ✅ |
| H3 | 教室不重叠 | 硬 | task | — | 两 task 同 room 同 slot → violation | ✅ |
| H4 | 教室容量 | 硬 | task | — | task.student_ids 数 > room.capacity → violation | ✅ |
| H5 | 课时排满 | 硬 | task | — | task 的 assignment 数 ≠ weekly_hours → violation | ✅ |
| H6 | 禁排 forbidden_slot | 硬 | teacher/course/task | `{slots[]}` | 目标出现在 params.slots → violation | ✅ |
| H7 | 教室类型匹配 | 硬 | task | — | course.required_room_type ≠ room.type → violation | ✅ |
| H8 | 教师日上限 | 硬 | teacher | `{max}`（默认取 teacher.max_per_day） | 某 teacher 某 day task 数 > max → violation（可配置改软） | ✅ |
| S1 | 优先排上午 prefer_morning | 软 | course | — | course.prefer_morning 的 task 排在 PM → +罚 | ✅ |
| S2 | 连堂 consecutive | 软 | course | `{min,max}` | 应连排 N 但未形成同日连续 N 段（不跨午休）→ +罚 | ✅ |
| S3 | AP 落走班时段 | 软 | task | — | AP task 的 slot 非 walk_block（混排）→ +罚 | ✅ |
| S5 | 课表分散均衡 | 软 | course | — | 同 course 同日 >1 次，或连天扎堆 → +罚 | ✅ |
| S6 | 尽量不排 avoid_slot | 软 | teacher/course | `{slots[]}` | 出现在 avoid slots → +罚 | 后期 |
| S7 | 多教师互斥 | 软 | teacher | `{ids[]}` | 互斥教师同 slot → +罚 | 后期 |
| H9 | 指定时段 require_slot | 硬(可软) | teacher/class | `{slots[]}` | 目标在所有指定 slot 都无排课 → violation（覆盖"班主任/某老师指定时间上课"） | 后期 |
| S9 | 教研活动禁排 research_activity_block | 硬 | teacher | `{slots[]}` | 教研时段给该老师排了课 → violation（本质=禁排老师） | 后期 |

**注**：教师日上限(H8) v0.1 文档原列软，本次定稿改硬（超过即违法），可经 config 降为软。
删除"班主任规则"特例 → 通用"教师规则"：任意教师可附加多条 scope=teacher 约束（禁排/优先/指定时段 require_slot/互斥/上限…），班主任需求=require_slot 实例，非特例；规则同样可附其他 scope。新增 H9 require_slot。S4/S8 编号空出。

**默认权重**（软约束，可配）：S1 优先上午 5、S2 连堂 8、S3 AP落走班 10、S5 分散 3。
**连堂(S2)语义**：weekly_hours 拆成若干同日连续块，每块长度 ∈ [min,max]、不跨午休；与分散(S5)共存靠权重平衡。
**分散(S5)罚**：同课同日 >1 次按次计罚 + 跨天分布方差。

---

## 6. 分班引擎规格

**变量**：每个 `(student, ap_course)` ∈ 选课数据 → `section_id`（分到该课的哪个 section）。

**硬约束**：
- 每生每选课 AP 恰分 1 个 section。
- 每个 section 人数 ≤ 对应教室容量（取该 room_type 下最小容量室为上限）。
- 每门 AP 的 section 数 ≤ `can_teach` 该课的老师数。
- **每个 AP section 必须分配一个 `can_teach` 该课的老师**（teacher_id），且该老师 AP 课时 + 其他课时 ≤ `max_per_week`。

**软目标**（加权求和，最小化）：
- 均衡：同门 AP 各 section 人数方差。
- 降冲突压力：冲突图色数（见下）。
- 并行友好：选课组合相同的学生尽量同套 section。
- 教师负载均衡：AP 课时在能教老师间尽量均衡，避免扎堆。

**冲突图色数代理**：
1. 节点 = 各 AP section；边 = 两 section 共享 ≥1 学生。
2. 贪心着色得色数近似 `chromatic`。
3. 与 `walk_slot_count`（走班时段总槽数）比较：`chromatic > walk_slot_count` → 溢出不可避免，标记 `overflow_expected=true`。

**算法**：贪心初分（按选课人数/容量开 section，按行政班均衡塞人）→ 教师分配（贪心匹配 `can_teach` + 负载均衡，不可行则增 section）→ 模拟退火（邻域：把一学生换到同课另一 section；合并/拆分 section；重分老师）。评估**只算代理指标，不跑排课**。秒级，`--candidates` 出多组。

**输出**：`ap_sections[]` + 对应 AP `teaching_tasks`（写入 state）+ `{color_number, walk_slot_count, overflow_expected}`。

---

## 7. 排课引擎规格

**变量**：每个 `TeachingTask` 按 `weekly_hours` 拆成 N 个 occurrence；变量 = 每个 occurrence → `slot_id`；room 由 `room_policy` 决定（pinned 固定；assign 选同 type 可用 room）。

**硬约束**：H1–H8（见 §5）。H5 体现为"每个 task 的 occurrence 全部分配且不重复 slot"。

**目标**：`minimize  M * Σ(hard_violations) + Σ(soft_penalty)`，`M` 大常数（如 1000）使硬违反趋 0。

**邻域动作**：
1. 重 assign 一个 occurrence 到另一 slot。
2. swap 两 occurrence 的 slot。
3. 改 assign 任务的 room（换同 type 另一 room）。

**算法**：
1. 初解：按"约束强度"排序任务（AP/连堂/禁排多的先），逐 occurrence 贪心塞最早可行 slot；pinned room 固定。
2. 优化：模拟退火（温度 `T0`→`0`，接受劣解概率 `exp(-Δ/M)`）；每 K 步检查中断标志。
3. 可选：CSP 回溯为 AP 段找一个可行起点（walk_block 内着色）。

**超时/中断**：默认 `--timeout 5`（秒），可配；每 N 步检查取消令牌，可中断返回当前最优。

**可复现**：`--seed <int>` 固定随机种子，同输入同种子 → 同输出（便于 LLM/脚本幂等调用、回归对拍）。

**solve 行为**：默认覆盖写 `state.assignments`（尊重 `locks` 中的固定槽）；`--keep` 保留现排课、仅排未锁定部分。

**locks**：locked task 的 slot 固定，排课不改动；`--respect-locks` 默认开。

**不可行**：返回 best-effort 部分解 + `hard_violations[]`（含 task/约束/原因），退出码 `4`；`tt show` 高亮违规。

---

## 8. 分班→排课 接口与手动调

- 分班写 `state.ap_sections` + 把 AP `teaching_tasks` 追加进 `state.teaching_tasks`。
- 必修 `teaching_tasks` 由 `tt solve sections`（或 `tt build-tasks`）从 `teaching_assignments` 静态生成。
- `tt lock --task X --slot D1P2`：固定某 task 某槽；排课/swap 不得改。
- `tt swap --task X --to D1P2`：手动改槽，立即 `validate` 重算冲突并返回。
- 任何手动操作后 `state.updated_at` 更新；`tt status` 反映最新违规/得分。

---

## 9. 不可行与输出策略

- `solve` 始终返回 best-effort（即使有违规），不抛"无解"中断。
- `hard_violations[]` 结构：`{constraint_id, type, task_ids[], slot?, reason}`。
- CLI：有硬违反 → exit 1（已出解但有冲突）；完全排不开关键任务 → exit 4。
- `show` 用色块标注违规行（红=硬违反，黄=软罚高）。
- 前端同此 JSON 渲染。

---

## 10. 工程结构、配置与测试

### 10.1 TS Monorepo（pnpm workspaces）
```
packages/
  core/      # 领域库：model(实体+zod schema) + solver(分班+排课) + validate + state(io)
             # 无 Node/浏览器 IO 依赖，纯逻辑，可被 cli 与 web 共享
  cli/       # Node CLI（citty），import core；结构化输出/退出码
  web/       # 前端，import core；solver 跑在 Web Worker
shared types 全在 core 导出
```
- 跨平台：Mac/Ubuntu 开发，Windows 运行；CLI 用 Node 直接跑或 `pkg/bun build` 打包单文件。
- 状态 IO 集中在 `core/state`，CLI 与 Web 走同一读写（Web 端用 File System Access API 或走一个本地小 server，MVP 可先用导入/导出 JSON）。

### 10.2 走班时段配置
`tt config set-walk-blocks --slots D1P6,D1P7,D3P6,D3P7,D5P6,D5P7` → 写 `state.config.walk_blocks`。`is_walk_block` 由它派生，无需冗余存。

### 10.3 测试策略
- **约束单测**：每条约束构造 合法/违反 各一例，断言评估结果。
- **算法对拍**：小规模（1 年级、2 班、几门课）手算课表 vs solver 输出，验证无硬违反。
- **真实数据回归**：四维导出 Excel → 转入 → 排课 → 比对关键指标（冲突数、得分）。
- **属性测试**：随机生成选课组合，断言 solver 输出永远满足所有硬约束（若 exit≠4）。

---

## 11. 与设计文档的差异/修订
- 新增 `Teacher` 实体、`TeachingAssignment`（教师分工输入）、`ApSelection` 显式建模。
- `教师日上限` 由软改硬（H8），可 config 降软。
- 删除"班主任规则"特例，泛化为通用"教师规则"：任意教师可附加多条 scope=teacher 约束；新增 `require_slot`(H9) 覆盖"指定时间上课"。规则可附任意 scope。
- 分班引擎补充**教师分配**：每个 AP section 分配 `can_teach` 老师 + 负载均衡，受 `max_per_week` 约束。
- 排课新增 `--seed`（可复现）、`--keep`（保留现排课）；新增 `tt validate-input` 输入预检（引用完整性/容量/教师超载）。
- 补默认软约束权重、连堂/分散语义。
- 明确排课变量为 occurrence→slot，room 按 policy pinned/assign。
- 明确状态文件 schema、CLI 命令契约、约束评估表、算法规格、不可行输出。

## 12. 仍待录入（运行时数据，不阻塞构建）
- 老师总数、教室数与类型、AP 门类清单与各自课时/容量/可教老师数、走班时段具体槽位、必修课归属明细。这些进入 `timetable.json` 即可，不影响 Core 实现。
